import React from 'react';
import useTitle from '../../Hooks/useTitle';

const Home = () => {
    useTitle('KidsCamp');
    return (
        <div>
            This is home
        </div>
    );
};

export default Home;